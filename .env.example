# Optional: enables live AI analysis in production and local development.
# If omitted, JobCraftor will fall back to its validated mock analysis engine.
OPENAI_API_KEY=

# Optional: override the default OpenAI model used by the server-side analyzer.
OPENAI_MODEL=gpt-4o-mini
